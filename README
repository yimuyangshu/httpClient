#### httpClient
```php
    $http = new httpClient($url);
	
	$http->set_headers(Arr);
	
	// 设置body参数
    $http->set_body(""); 
	
    $http->set_param(Arr);
	
    $result = $http->post(); // or get();
```